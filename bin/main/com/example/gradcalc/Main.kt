package com.example.gradcalc

/**
 * Entry point stub. Compose UI has been removed from this build to keep
 * dependencies minimal.  The CLI logic is implemented in [ConsoleApp].
 */
fun main() {
    println("UI not included. Run the console application instead.")
}
